import VBtn from '../VBtn.vue';
console.log(VBtn);

/*
describe('VBtn.vue', () => {
  it('renders a button', () => {
    const wrapper = mount(VBtn);
    expect(wrapper.html()).toMatchSnapshot();
  });
});
*/
